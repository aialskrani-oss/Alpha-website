kind = "web"
previewPath = "/"
title = "Alpha Platform"
version = "1.0.0"
id = "artifacts/alpha-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18568

[services.development]
run = "pnpm --filter @workspace/alpha-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/alpha-platform", "run", "build" ]
publicDir = "artifacts/alpha-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18568"
BASE_PATH = "/"
